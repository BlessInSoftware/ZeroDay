name: CD

on:
  push:
    branches:
      - main
    tags:
      - '*'

jobs:
  build:
    permissions:
      contents: read
    uses: ./.github/workflows/docker-build.yml

  publish:
    needs: build
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/docker-publish.yml
